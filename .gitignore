node_modules/
dist/
.DS_Store
*.local

# SAM build artifacts
.aws-sam/

# analytics-report: bundled DB + deps (fetch before deploy, see ANALYTICS.md)
analytics-report/src/node_modules/
analytics-report/src/*.mmdb
analytics-report/src/*.mmdb.gz
analytics-report/src/package-lock.json

# playwright MCP scratch (screenshots, snapshots)
.playwright-mcp/
